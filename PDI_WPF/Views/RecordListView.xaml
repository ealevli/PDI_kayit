<UserControl x:Class="PDI_WPF.Views.RecordListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="White" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="PDI KAYIT LİSTESİ" FontSize="20" FontWeight="Bold" Margin="0,0,0,20" />

        <!-- Filtreler Paneli -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="15" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="Şasi Ara:" FontSize="11" Margin="0,0,0,5" />
                    <TextBox Width="150"
                        Text="{Binding SearchSasi, UpdateSourceTrigger=PropertyChanged}" Padding="5" />
                </StackPanel>
                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="Araç Tipi:" FontSize="11" Margin="0,0,0,5" />
                    <ComboBox Width="120" Padding="5">
                        <ComboBoxItem Content="Hepsi" IsSelected="True" />
                        <ComboBoxItem Content="Travego" />
                        <ComboBoxItem Content="Tourismo" />
                    </ComboBox>
                </StackPanel>
                <Button Content="YENİLE" Command="{Binding RefreshCommand}"
                    Style="{StaticResource SidebarButtonStyle}" Width="100"
                    VerticalAlignment="Bottom" />
            </StackPanel>
        </Border>

        <!-- Kayıt Tablosu -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}">
            <DataGrid ItemsSource="{Binding Records}" AutoGenerateColumns="False" IsReadOnly="True"
                HeadersVisibility="Column" GridLinesVisibility="Horizontal" SelectionUnit="FullRow">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" />
                    <DataGridTextColumn Header="BB No" Binding="{Binding BbNo}" Width="80" />
                    <DataGridTextColumn Header="Şasi No" Binding="{Binding SasiNo}" Width="120" />
                    <DataGridTextColumn Header="Araç Tipi" Binding="{Binding AracTipi}" Width="100" />
                    <DataGridTextColumn Header="Tespitler" Binding="{Binding Tespitler}" Width="2*" />
                    <DataGridTextColumn Header="Top Hata" Binding="{Binding TopHata}" Width="100" />
                    <DataGridTextColumn Header="Tarih" Binding="{Binding TarihSaat}" Width="130" />

                    <!-- İşlemler Sütunu -->
                    <DataGridTemplateColumn Header="İşlemler" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="✎" Margin="2" Background="Transparent"
                                        BorderThickness="0" Foreground="Blue" />
                                    <Button Content="🗑" Margin="2" Background="Transparent"
                                        BorderThickness="0" Foreground="Red" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>