<UserControl x:Class="PDI_WPF.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#000000">
        <Border Width="450" Height="500" Background="White" CornerRadius="10" Padding="40" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Mercedes-Benz" FontSize="28" FontWeight="Bold" FontFamily="Times New Roman" HorizontalAlignment="Center"/>
                <TextBlock Text="BUS PDI SYSTEM" FontSize="12" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#666" Margin="0,0,0,40"/>
                <TextBlock Text="Kullanıcı Adı" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Username}" FontSize="14" Padding="10" Margin="0,0,0,20"/>
                <TextBlock Text="Şifre" FontWeight="Bold" Margin="0,0,0,5"/>
                <PasswordBox x:Name="txtPass" FontSize="14" Padding="10" Margin="0,0,0,30"/>
                <Button Content="GİRİŞ YAP" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=txtPass}"
                        Background="Black" Foreground="White" FontSize="14" FontWeight="Bold" Padding="15" BorderThickness="0"/>
                <TextBlock Text="Admin: admin / admin123" Foreground="#CCC" FontSize="10" HorizontalAlignment="Center" Margin="0,20,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
