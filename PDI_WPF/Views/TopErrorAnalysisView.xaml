<UserControl x:Class="PDI_WPF.Views.TopErrorAnalysisView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="White" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Başlık ve Filtreler -->
        <Grid Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="{Binding Title}" FontSize="24" FontWeight="Bold" />
                <TextBlock Text="{Binding FilterInfo}" Foreground="#666" FontSize="14"
                    Margin="0,5,0,0" />
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <TextBlock Text="Ay:" VerticalAlignment="Center" Margin="0,0,5,0" />
                <ComboBox ItemsSource="{Binding MonthList}"
                    SelectedIndex="{Binding SelectedMonth, Mode=TwoWay, Converter={StaticResource MonthIndexConverter}}"
                    Width="100" Padding="5" Margin="0,0,10,0" />

                <TextBlock Text="Yıl:" VerticalAlignment="Center" Margin="0,0,5,0" />
                <ComboBox ItemsSource="{Binding YearList}"
                    SelectedItem="{Binding SelectedYear}"
                    Width="80" Padding="5" Margin="0,0,10,0" />

                <Button Content="🔄 Yenile"
                    Command="{Binding RefreshCommand}"
                    Padding="10,5" Background="#f8f9fa" Cursor="Hand" />
            </StackPanel>
        </Grid>

        <!-- Açıklama -->
        <Border Grid.Row="1" Background="#e3f2fd" Padding="15" Margin="0,0,0,20" CornerRadius="5">
            <TextBlock TextWrapping="Wrap" Foreground="#1565c0">
                <Run Text="ℹ️" FontSize="16" />
                <Run
                    Text=" Bu tablo, seçilen ay için TRV (Travego) ve TOU (Tourismo) araçlarında görülen Top Hataların yüzdesel dağılımını gösterir. Satıra tıklayarak detaylı analiz sayfasına gidebilirsiniz." />
            </TextBlock>
        </Border>

        <!-- Ana Tablo -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}">
            <DataGrid ItemsSource="{Binding AnalysisData}"
                AutoGenerateColumns="False"
                IsReadOnly="True"
                SelectionMode="Single"
                CanUserAddRows="False"
                GridLinesVisibility="Horizontal"
                HeadersVisibility="Column"
                RowHeight="40"
                FontSize="13">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Top Hata" Binding="{Binding HataAdi}" Width="*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="SemiBold" />
                                <Setter Property="VerticalAlignment" Value="Center" />
                                <Setter Property="Padding" Value="10,0" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="TRV %"
                        Binding="{Binding TrvRate, StringFormat='{}{0:F2}%'}" Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center" />
                                <Setter Property="VerticalAlignment" Value="Center" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="TOU %"
                        Binding="{Binding TouRate, StringFormat='{}{0:F2}%'}" Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center" />
                                <Setter Property="VerticalAlignment" Value="Center" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Genel AVG"
                        Binding="{Binding AvgRate, StringFormat='{}{0:F2}%'}" Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center" />
                                <Setter Property="VerticalAlignment" Value="Center" />
                                <Setter Property="FontWeight" Value="Bold" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Durum" Binding="{Binding DurumText}" Width="150">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center" />
                                <Setter Property="Foreground" Value="{Binding DurumColor}" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>

                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Cursor" Value="Hand" />
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#f8f9fa" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>